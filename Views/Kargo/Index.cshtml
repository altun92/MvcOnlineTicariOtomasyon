@model List<MvcOnlineTicariOtomasyon.Models.Siniflar.KargoDetay>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}

<br />
<h2>Kargolar</h2>
<br />
@using (Html.BeginForm("Index", "Kargo", FormMethod.Get))
{
    <p>
        Kargo Takip Numarası: @Html.TextBox("p")
        <input type="submit" value="Ara" />
    </p>
}
<table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
    <a href="/Kargo/KargoEkle" class="btn btn-primary">Yeni Kargo</a>
    <br />
    <br />
    <thead>
        <tr>
            <th>Sıra No</th>
            <th>Takip Kodu</th>
            <th>Personel</th>
            <th>Alıcı</th>
            <th>Tarih</th>
            <th>Detaylar</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            count++;
            <tr>
                <td>@count</td>
                <td>@item.TakipKodu</td>
                <td>@item.Personel</td>
                <td>@item.Alici</td>
                <td>@item.Tarih.ToShortDateString()</td>
                <td>@item.Aciklama</td>
                <td>
                    <a href="/Kargo/KargoDetay/@item.TakipKodu" class="btn btn-warning">Kargo Hareketleri</a>
                </td>
            </tr>
        }
    </tbody>

</table>
